# Noon UAE ASN 实机验收记录

验收日期：2026-08-11

## 单笔契约捕获

- 使用输入目录中 SKU 行数最少的一份表格制作临时副本，原文件未修改。
- 店铺：店铺1；项目：`PRJ42958`；行项目数：1。
- 通过 API JWT 获取新 Cookie，在隔离的可见 Chrome 中执行一次授权创建。
- 创建结果：`A0590***PN`。
- Noon 详情接口核验：项目一致、合作方一致、完整 SKU 集合一致、全部数量一致。
- 仅在上述核验通过后，将完整 ASN 写入临时副本 `约仓!C2`；重新读取确认一致。
- 对同一临时副本连续运行正式入口两次，结果均为 `skipped_existing`，退出码均为 `0`。

## 契约与安全

- 已捕获并模板化：ASN 列表、目录 SKU 查询、ASN 主单创建、仓库路由、全量行创建、库容检查、ASN 详情。
- 契约扫描确认不含本次 SKU、完整 ASN、私钥、JWT、Cookie 值或会话字符串。
- 原始认证捕获只写入 `%LOCALAPPDATA%\NoonASNCreator\capture\live-*` 受限目录，契约验证后已删除。
- API 只读验收检查最新 10 个 ASN，唯一精确匹配项与临时副本完全一致。

## 网页稳定性观察

Noon 创建页曾返回已失效的 Next.js 页面分块，导致脚本 404、主区域空白；刷新后恢复。浏览器兜底已按关键业务控件检测并允许有限刷新，不依赖 `networkidle` 或坐标点击。正常运行优先走内部 API，不承担该页面加载等待。

## 28 文件整批验收

输入目录在运行前完成独立备份和 SHA-256 清单。验收过程中发现详情接口的 `perPage: 10` 会截断大 ASN，已改为 100；同时补齐 Noon 官方创建页必经的仓库路由检查和全量行提交。

| # | 验收项 | 结果 |
|---|---|---|
| 1 | 顶层 `.xlsx` 数量 | 28，运行前后不变 |
| 2 | 初始 `C2` | 28 个均为空 |
| 3 | 文件隔离 | 单文件失败未中断后续文件 |
| 4 | 写回范围 | OOXML 对比确认 28 个文件除 `约仓!C2` 外无变化 |
| 5 | 已完成文件 | 26 个 `C2` 已写入 |
| 6 | 待拆分文件 | 04、06 共 2 个，Noon 路由返回 `split_required` |
| 7 | 大 ASN 详情 | 15 行和 22 行 ASN 以 `perPage: 100` 精确核验通过 |
| 8 | 空行 ASN | 详情省略 `lines` 时按 0 行安全处理 |
| 9 | 路由顺序 | `main -> route -> full lines -> storage -> details` 与 Noon 前端一致 |
| 10 | 幂等恢复 | 有 `pendingAsn` 时网络失败不清零 `attempts`、不再次创建 |
| 11 | 自动测试 | 71 项全部通过，TypeScript 类型检查通过 |
| 12 | EXE | 无 Node 冒烟测试和打包 Chrome 启动测试通过 |

代表性工作簿已通过独立电子表格渲染检查：03、13 的 ASN 在 `C2` 可见，04 保持空白，版式正常且公式错误为 0。

六份 API JWT 登录接口均返回 200，Cookie 名称集合一致。最终逐单复核中，店铺3、4共 8 个 ASN 再次精确匹配；店铺1、2、5、6共 18 个在 FBN 详情接口返回 401。03 和 13 在会话失效前已分别以 15/15、22/22 行精确核验；其余已写项来自运行时逐单详情核验。该差异表明 Noon 登录成功不等于当前 key 对 FBN 详情接口持续有效，验收报告保留在 Git 外部目录。

06 的脱敏路由诊断确认：13 行中有 6 行 `apparel_footwear`、7 行 `unidentified`。前者单独路由返回 200；后者全部缺少尺寸分类和正体积，单独路由仍返回 `Split ASN`。程序现已在主单创建前拒绝 `unidentified` 商品，要求先补齐 Noon 商品尺寸，避免再生成空 ASN。04 因店铺5 FBN 接口 401，尚未取得同等粒度的目录属性诊断。

验收中曾暴露并修复 `failed -> validated` 清零尝试次数的缺陷；该缺陷在修复前产生了不完整重复 ASN。重复单未写入工作簿，需在 Noon 后台人工核对并取消，取消动作不属于本工具的自动权限范围。
